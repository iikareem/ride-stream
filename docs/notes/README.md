# RideStream notes

Learning notes for the live location pipeline: Kafka, Avro, ksqlDB, Redis, and the WebSocket gateway.

Start at [RideStream](RideStream.md).

The code is [github.com/iikareem/ride-stream](https://github.com/iikareem/ride-stream). These notes match that repo. Grafana `admin` / `admin` in the observability note is the local demo login, not a production secret.
