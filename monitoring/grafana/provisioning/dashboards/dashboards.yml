apiVersion: 1

providers:
  - name: ridestream
    orgId: 1
    folder: RideStream
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
